package kingdom.smp.client;

import net.minecraft.ChatFormatting;
import net.minecraft.client.Minecraft;
import net.minecraft.client.gui.screens.inventory.AbstractContainerScreen;
import net.minecraft.client.gui.screens.inventory.CrafterScreen;
import net.minecraft.network.chat.Component;
import net.neoforged.bus.api.SubscribeEvent;
import net.neoforged.neoforge.client.event.ScreenEvent;
import org.lwjgl.glfw.GLFW;

/**
 * F4 / Tab / arrow / drag debug overlay for the Ironhold Crafter reskin.
 * Generated by scripts/gen_gui_reskins.py.
 */
public final class CrafterSlotDebugHandler {
    private CrafterSlotDebugHandler() {}

    private static boolean isCrafter(Object screen) {
        return screen instanceof CrafterScreen;
    }

    @SubscribeEvent
    public static void onKey(ScreenEvent.KeyPressed.Pre event) {
        if (!isCrafter(event.getScreen())) return;
        int key = event.getKeyEvent().key();
        boolean shift = (event.getKeyEvent().modifiers() & GLFW.GLFW_MOD_SHIFT) != 0;

        if (key == GLFW.GLFW_KEY_F4) {
            CrafterSlotDebug.toggleEditMode();
            chat("§b[ironhold/crafter] edit mode " + (CrafterSlotDebug.isEditMode() ? "ON" : "OFF"));
            event.setCanceled(true);
            return;
        }
        if (!CrafterSlotDebug.isEditMode()) return;
        int step = shift ? 5 : 1;
        switch (key) {
            case GLFW.GLFW_KEY_TAB -> { CrafterSlotDebug.cycleSelection(shift ? -1 : 1); chat(CrafterSlotDebug.summary()); }
            case GLFW.GLFW_KEY_LEFT  -> { CrafterSlotDebug.nudge(-step, 0); chat(CrafterSlotDebug.summary()); }
            case GLFW.GLFW_KEY_RIGHT -> { CrafterSlotDebug.nudge( step, 0); chat(CrafterSlotDebug.summary()); }
            case GLFW.GLFW_KEY_UP    -> { CrafterSlotDebug.nudge(0, -step); chat(CrafterSlotDebug.summary()); }
            case GLFW.GLFW_KEY_DOWN  -> { CrafterSlotDebug.nudge(0,  step); chat(CrafterSlotDebug.summary()); }
            case GLFW.GLFW_KEY_P -> {
                String dump = CrafterSlotDebug.printJava();
                System.out.println(dump);
                for (String line : dump.split("\n")) chat(line);
            }
            case GLFW.GLFW_KEY_R -> { CrafterSlotDebug.resetAll(); chat("§e[ironhold/crafter] reset to defaults"); }
            default -> { return; }
        }
        event.setCanceled(true);
    }

    @SubscribeEvent
    public static void onMouseDown(ScreenEvent.MouseButtonPressed.Pre event) {
        if (!isCrafter(event.getScreen())) return;
        if (!CrafterSlotDebug.isEditMode()) return;
        if (event.getMouseButtonEvent().button() != 0) return;
        AbstractContainerScreen<?> s = (AbstractContainerScreen<?>) event.getScreen();
        int mx = (int) event.getMouseX();
        int my = (int) event.getMouseY();
        if (CrafterSlotDebug.tryStartDrag(mx, my, s.getGuiLeft(), s.getGuiTop())) {
            event.setCanceled(true);
        }
    }

    @SubscribeEvent
    public static void onMouseDrag(ScreenEvent.MouseDragged.Pre event) {
        if (!isCrafter(event.getScreen())) return;
        if (!CrafterSlotDebug.isDragging()) return;
        AbstractContainerScreen<?> s = (AbstractContainerScreen<?>) event.getScreen();
        int mx = (int) event.getMouseX();
        int my = (int) event.getMouseY();
        CrafterSlotDebug.updateDrag(mx, my, s.getGuiLeft(), s.getGuiTop());
        event.setCanceled(true);
    }

    @SubscribeEvent
    public static void onMouseUp(ScreenEvent.MouseButtonReleased.Pre event) {
        if (!isCrafter(event.getScreen())) return;
        if (!CrafterSlotDebug.isDragging()) return;
        CrafterSlotDebug.endDrag();
        chat(CrafterSlotDebug.summary());
        event.setCanceled(true);
    }

    private static void chat(String msg) {
        Minecraft mc = Minecraft.getInstance();
        if (mc.player != null) mc.player.sendSystemMessage(Component.literal(msg).withStyle(ChatFormatting.AQUA));
    }
}

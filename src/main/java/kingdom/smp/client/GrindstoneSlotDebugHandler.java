package kingdom.smp.client;

import net.minecraft.ChatFormatting;
import net.minecraft.client.Minecraft;
import net.minecraft.client.gui.screens.inventory.AbstractContainerScreen;
import net.minecraft.client.gui.screens.inventory.GrindstoneScreen;
import net.minecraft.network.chat.Component;
import net.neoforged.bus.api.SubscribeEvent;
import net.neoforged.neoforge.client.event.ScreenEvent;
import org.lwjgl.glfw.GLFW;

/**
 * F4 / Tab / arrow / drag debug overlay for the Ironhold Grindstone reskin.
 * Generated by scripts/gen_gui_reskins.py.
 */
public final class GrindstoneSlotDebugHandler {
    private GrindstoneSlotDebugHandler() {}

    private static boolean isGrindstone(Object screen) {
        return screen instanceof GrindstoneScreen;
    }

    @SubscribeEvent
    public static void onKey(ScreenEvent.KeyPressed.Pre event) {
        if (!isGrindstone(event.getScreen())) return;
        int key = event.getKeyEvent().key();
        boolean shift = (event.getKeyEvent().modifiers() & GLFW.GLFW_MOD_SHIFT) != 0;

        if (key == GLFW.GLFW_KEY_F4) {
            GrindstoneSlotDebug.toggleEditMode();
            chat("§b[ironhold/grindstone] edit mode " + (GrindstoneSlotDebug.isEditMode() ? "ON" : "OFF"));
            event.setCanceled(true);
            return;
        }
        if (!GrindstoneSlotDebug.isEditMode()) return;
        int step = shift ? 5 : 1;
        switch (key) {
            case GLFW.GLFW_KEY_TAB -> { GrindstoneSlotDebug.cycleSelection(shift ? -1 : 1); chat(GrindstoneSlotDebug.summary()); }
            case GLFW.GLFW_KEY_LEFT  -> { GrindstoneSlotDebug.nudge(-step, 0); chat(GrindstoneSlotDebug.summary()); }
            case GLFW.GLFW_KEY_RIGHT -> { GrindstoneSlotDebug.nudge( step, 0); chat(GrindstoneSlotDebug.summary()); }
            case GLFW.GLFW_KEY_UP    -> { GrindstoneSlotDebug.nudge(0, -step); chat(GrindstoneSlotDebug.summary()); }
            case GLFW.GLFW_KEY_DOWN  -> { GrindstoneSlotDebug.nudge(0,  step); chat(GrindstoneSlotDebug.summary()); }
            case GLFW.GLFW_KEY_P -> {
                String dump = GrindstoneSlotDebug.printJava();
                System.out.println(dump);
                for (String line : dump.split("\n")) chat(line);
            }
            case GLFW.GLFW_KEY_R -> { GrindstoneSlotDebug.resetAll(); chat("§e[ironhold/grindstone] reset to defaults"); }
            default -> { return; }
        }
        event.setCanceled(true);
    }

    @SubscribeEvent
    public static void onMouseDown(ScreenEvent.MouseButtonPressed.Pre event) {
        if (!isGrindstone(event.getScreen())) return;
        if (!GrindstoneSlotDebug.isEditMode()) return;
        if (event.getMouseButtonEvent().button() != 0) return;
        AbstractContainerScreen<?> s = (AbstractContainerScreen<?>) event.getScreen();
        int mx = (int) event.getMouseX();
        int my = (int) event.getMouseY();
        if (GrindstoneSlotDebug.tryStartDrag(mx, my, s.getGuiLeft(), s.getGuiTop())) {
            event.setCanceled(true);
        }
    }

    @SubscribeEvent
    public static void onMouseDrag(ScreenEvent.MouseDragged.Pre event) {
        if (!isGrindstone(event.getScreen())) return;
        if (!GrindstoneSlotDebug.isDragging()) return;
        AbstractContainerScreen<?> s = (AbstractContainerScreen<?>) event.getScreen();
        int mx = (int) event.getMouseX();
        int my = (int) event.getMouseY();
        GrindstoneSlotDebug.updateDrag(mx, my, s.getGuiLeft(), s.getGuiTop());
        event.setCanceled(true);
    }

    @SubscribeEvent
    public static void onMouseUp(ScreenEvent.MouseButtonReleased.Pre event) {
        if (!isGrindstone(event.getScreen())) return;
        if (!GrindstoneSlotDebug.isDragging()) return;
        GrindstoneSlotDebug.endDrag();
        chat(GrindstoneSlotDebug.summary());
        event.setCanceled(true);
    }

    private static void chat(String msg) {
        Minecraft mc = Minecraft.getInstance();
        if (mc.player != null) mc.player.sendSystemMessage(Component.literal(msg).withStyle(ChatFormatting.AQUA));
    }
}

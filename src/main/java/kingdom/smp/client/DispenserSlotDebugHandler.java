package kingdom.smp.client;

import net.minecraft.ChatFormatting;
import net.minecraft.client.Minecraft;
import net.minecraft.client.gui.screens.inventory.AbstractContainerScreen;
import net.minecraft.client.gui.screens.inventory.DispenserScreen;
import net.minecraft.network.chat.Component;
import net.neoforged.bus.api.SubscribeEvent;
import net.neoforged.neoforge.client.event.ScreenEvent;
import org.lwjgl.glfw.GLFW;

/**
 * F4 / Tab / arrow / drag debug overlay for the Ironhold Dispenser reskin.
 * Generated by scripts/gen_gui_reskins.py.
 */
public final class DispenserSlotDebugHandler {
    private DispenserSlotDebugHandler() {}

    private static boolean isDispenser(Object screen) {
        return screen instanceof DispenserScreen;
    }

    @SubscribeEvent
    public static void onKey(ScreenEvent.KeyPressed.Pre event) {
        if (!isDispenser(event.getScreen())) return;
        int key = event.getKeyEvent().key();
        boolean shift = (event.getKeyEvent().modifiers() & GLFW.GLFW_MOD_SHIFT) != 0;

        if (key == GLFW.GLFW_KEY_F4) {
            DispenserSlotDebug.toggleEditMode();
            chat("§b[ironhold/dispenser] edit mode " + (DispenserSlotDebug.isEditMode() ? "ON" : "OFF"));
            event.setCanceled(true);
            return;
        }
        if (!DispenserSlotDebug.isEditMode()) return;
        int step = shift ? 5 : 1;
        switch (key) {
            case GLFW.GLFW_KEY_TAB -> { DispenserSlotDebug.cycleSelection(shift ? -1 : 1); chat(DispenserSlotDebug.summary()); }
            case GLFW.GLFW_KEY_LEFT  -> { DispenserSlotDebug.nudge(-step, 0); chat(DispenserSlotDebug.summary()); }
            case GLFW.GLFW_KEY_RIGHT -> { DispenserSlotDebug.nudge( step, 0); chat(DispenserSlotDebug.summary()); }
            case GLFW.GLFW_KEY_UP    -> { DispenserSlotDebug.nudge(0, -step); chat(DispenserSlotDebug.summary()); }
            case GLFW.GLFW_KEY_DOWN  -> { DispenserSlotDebug.nudge(0,  step); chat(DispenserSlotDebug.summary()); }
            case GLFW.GLFW_KEY_P -> {
                String dump = DispenserSlotDebug.printJava();
                System.out.println(dump);
                for (String line : dump.split("\n")) chat(line);
            }
            case GLFW.GLFW_KEY_R -> { DispenserSlotDebug.resetAll(); chat("§e[ironhold/dispenser] reset to defaults"); }
            default -> { return; }
        }
        event.setCanceled(true);
    }

    @SubscribeEvent
    public static void onMouseDown(ScreenEvent.MouseButtonPressed.Pre event) {
        if (!isDispenser(event.getScreen())) return;
        if (!DispenserSlotDebug.isEditMode()) return;
        if (event.getMouseButtonEvent().button() != 0) return;
        AbstractContainerScreen<?> s = (AbstractContainerScreen<?>) event.getScreen();
        int mx = (int) event.getMouseX();
        int my = (int) event.getMouseY();
        if (DispenserSlotDebug.tryStartDrag(mx, my, s.getGuiLeft(), s.getGuiTop())) {
            event.setCanceled(true);
        }
    }

    @SubscribeEvent
    public static void onMouseDrag(ScreenEvent.MouseDragged.Pre event) {
        if (!isDispenser(event.getScreen())) return;
        if (!DispenserSlotDebug.isDragging()) return;
        AbstractContainerScreen<?> s = (AbstractContainerScreen<?>) event.getScreen();
        int mx = (int) event.getMouseX();
        int my = (int) event.getMouseY();
        DispenserSlotDebug.updateDrag(mx, my, s.getGuiLeft(), s.getGuiTop());
        event.setCanceled(true);
    }

    @SubscribeEvent
    public static void onMouseUp(ScreenEvent.MouseButtonReleased.Pre event) {
        if (!isDispenser(event.getScreen())) return;
        if (!DispenserSlotDebug.isDragging()) return;
        DispenserSlotDebug.endDrag();
        chat(DispenserSlotDebug.summary());
        event.setCanceled(true);
    }

    private static void chat(String msg) {
        Minecraft mc = Minecraft.getInstance();
        if (mc.player != null) mc.player.sendSystemMessage(Component.literal(msg).withStyle(ChatFormatting.AQUA));
    }
}

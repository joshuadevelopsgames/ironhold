package kingdom.smp.rpg;

/** One row in the profile Skills section (placeholders until keybinds + server logic). */
public record ClassSkillEntry(String title, String category, String description, String status) {}
